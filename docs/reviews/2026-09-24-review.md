# Ryan Moore — feedback, sweep of 2026-09-24

One stage is in this one, Stage 1.2, and this time the workbook moved. The bed cells hold the solved mix, the full Solver model is saved with its seven constraints, and the three typed displays in your specification are derivations now, with the frontmatter reading `audited` — the model finally obeys the Conventions you wrote for it. What has not caught up is the audit: its findings still say the workbook is not yet solved, which was true when you wrote them and is not any more. Rewrite it for the solved build — three findings in your Checked / Found / Did form, including what a second starting point returned — then open the file in Excel, recalculate, save and commit, so the values are cached for anyone who opens it. Twenty minutes and two. Push those and reply in the pull-request thread with what you changed.

## Stage 1.2 — Spec, Build, Audit

Reviewed below, criterion by criterion. This is entered.

**CRITERION BY CRITERION**

* **Spec completeness — inputs, structure, calculation flow** — Up five: all three typed displays are derivations now, and the frontmatter reads `audited`. Open: `DimReturns` at spec lines 35–37 read 10 / 2.5 / 1.25 while the workbook holds decimals.
* **Spec validation rules** — Up one for the exact hand check — one tomato bed = 99 hours, matching the model. Open: the acceptance profit is "approximately $42,775" with no band, against your own workbook's $42,761.66, and the line still says "not against a Solver run".
* **Workbook satisfies the contract** — Up eight, the row that carried the re-grade: `Calc!B4:B6` read 10 / 20 / 30 and the full Solver model is saved with its seven constraints. Three held, one each: no cached values, no two-start run recorded, a stray empty Solver model on the Output sheet.
* **Audit note** — Unchanged, and now stale: the findings still say the workbook is not yet solved.

**Repo:** https://github.com/ryanmoor/ryan-moore

**Workbook:** `capabilities/marginal-analysis/model.xlsx` (**9,478 B** at `42e3927`, 2026-09-22 — solved at 10 / 20 / 30, Solver model saved; 25 formulas, none cached) · *09-21:* (8,709 B, blob `a2c2b78` — **unchanged that sweep**; 3 sheets, 22 formulas, 0 error cells, **40 named ranges**) — now this case, unsolved · `README.md` (767 B) · brief now a real `.md` file (3,965 B)

**WHAT I'D FIX FIRST**

* Rewrite the audit for the solved build: three findings in your Checked / Found / Did form — what Solver returned, what the second starting point returned, what it means for the decision. Twenty minutes, and the largest gap left.
* Open `model.xlsx` in Excel, recalculate, save, commit, and delete the empty Solver model on the Output sheet while you are in it. Two minutes.
* Run Solver from a second starting point — 0 / 0 / 0 or 20 / 0 / 0 — and record both results. Five minutes.
* Set the acceptance figure to $42,761.66 with a tolerance and drop the "not against a Solver run" line.
* State `DimReturns` as 0.10 / 0.025 / 0.0125 so the contract and the build agree on units.

**LOOKING AHEAD**

The model now obeys the Conventions you wrote, and that is what the score records. Stage 1.3 reads its numbers from this workbook, so the cached values matter there too. Push the fixes, then reply in the pull-request thread with what you changed.

---
