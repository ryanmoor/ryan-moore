# Ryan Moore — feedback, sweep of 2026-09-21

One stage moved for you this sweep, and it moved in one half of itself. The specification is rewritten and it is now this farm's model end to end — inputs, structure, conventions, validation — which is the harder half of the stage to fake. The workbook it describes is the same file I read last time, with nothing planted in it, and closing that gap is what the part below is about.

## Stage 1.2 — Spec, Build, Audit

Reviewed below, criterion by criterion. This is entered.

**CRITERION BY CRITERION**

* **Spec completeness — inputs, structure, calculation flow** — The generic break-even body is gone and the document is this case end to end: a 23-row named input contract with value, unit and source, a Structure section naming the three sheets and which cells Solver's constraints reference, the labor tiering written out as `OwnLaborHoursUsed = MIN(TotalLaborHours, OwnLaborHours)`, and eight conventions of your own. Deductions: `OwnWageRate` 34.72, `TempWageRate` 17.36 and `LaborHrsPerBedCarrots` 0.833 are typed displays rather than derivations, the diminishing-returns rows are given as percents where the workbook holds decimals, and the frontmatter still says `status: draft`.
* **Spec validation rules** — The biggest move in your section, and it is earned. The blank pasted template is gone: every calculated cell must be a named-range formula except the three decision cells, no error cells at any feasible point, a hand check at 0/0/0 and another at 10/20/30 whose 5,276.8 labor hours agree with my model's 5,277.2, and integer and non-negativity required in the Solver model itself rather than left to convention. Deductions: your acceptance profit is stated as about $42,775 — your own rounded-input figure rather than the published $42,761.66 — and both hand checks use an approximately-equal sign with no band.
* **Workbook satisfies the contract** — Byte-identical to the file I read last sweep; I reopened it to be sure. What is right: 40 named ranges, every case input present and correct, the labor function and the MIN/MAX tiering implemented as named-range formulas, and the full Solver dialog written out on the Output sheet. What is not: Solver has never been run on it, the bed cells read 0 / 0 / 0, and the workbook reports a loss rather than a decision.
* **Audit note** — Three findings, each in checked / found / did form, and the first is genuinely good: an independent Python replica of your `Calc` formulas evaluated at four bed mixes, with the all-zero case matching minus fixed costs exactly and the over-cap mix going sharply negative as the compounding term predicts. The third — no formula referencing a raw cell address, no unused name — is a check most people skip. The deduction is that none of the three audits a solved model, so no finding touches the decision, the profit figure or the crossover behavior.

**Repo:** https://github.com/ryanmoor/ryan-moore
**Spec:** `capabilities/marginal-analysis/spec.md` (**8,399 B** — was 4,823)
**Workbook:** `capabilities/marginal-analysis/model.xlsx` (8,709 B, blob `a2c2b78` — **unchanged this sweep**; 3 sheets, 22 formulas, 0 error cells, **40 named ranges**) — now this case, unsolved · `README.md` (767 B) · brief now a real `.md` file (3,965 B)

**THE MODEL STILL SOLVES TO NOTHING**

Your three bed counts read 0, 0 and 0. Nothing is planted. All 22 formula cells in the file are uncached, so it opens blank; recalculate it and profit reads minus $20,000, which is your fixed costs and nothing else.

Solver has never been run on it. There are no Solver settings saved in the file at all, which means the dialog you wrote out so carefully on the Output sheet — objective, changing cells, the four constraints, the integer requirement — exists as text in a sheet and not as a model Excel can execute.

I am not going to soften that. The deliverable for the build half of this stage is a workbook that reports a decision, and yours reports a loss. That is the whole of the 14 out of 25. Opening the file, entering that dialog and pressing Solve is a few minutes of work, and the specification you have just written tells you exactly what to enter.

**WHAT I'D FIX FIRST**

* Run Solver and commit the solved file. Everything else here is worth less than this.
* Fix your acceptance target before you run it. Your spec asks for about $42,775 — but that is your own figure, produced by typing the rounded inputs, not the case's published $42,761.66. A target built out of the error cannot catch the error. Set it to $42,761.66 and give it a band; both your hand checks currently use an approximately-equal sign with no tolerance at all, which means neither one can fail.
* Derive `OwnWageRate`, `TempWageRate` and `LaborHrsPerBedCarrots` instead of typing 34.72, 17.36 and 0.833. Both rates are a salary over 1,440 hours and carrot hours are 2.5/3. That is the entire $13 gap.
* Settle the diminishing-returns units. Your spec gives them as 10, 2.5 and 1.25 "percent per additional bed" and the workbook holds them as decimals. One of the two misleads a reader, and the contract is the one that has to be unambiguous.

**LOOKING AHEAD**

The spec rewrite is real work and it is the half of this stage that is harder to fake. Your Conventions section in particular — eight rules of your own about hours pooled farm-wide, integer beds enforced as a constraint rather than asserted, fixed costs never prorated, hours beyond the cap infeasible rather than priced — is the part I would point at as good. What it is missing is a model that obeys it, and you are a few minutes from having one. One housekeeping item worth doing at the same time: add adamwstauffer as a collaborator on your repository, under Settings and then Collaborators, so the next review can be anchored to the cell it is about.

---
